name: Separació del diccionari (i generació de nàufragues i heptasíl·labs)

on:
  push:
    paths:
      - 'diccionaris/diccionari*.txt'
  workflow_dispatch:

jobs:
  update-general-version:
    runs-on: ubuntu-latest
    steps:
      - name: Baixa el codi
        uses: actions/checkout@v4

# ------ Separar diccionari (i preparar la generació de [500]col_10.txt) i POSTPROCÉS (generació [500]col_10.txt)
      - name: Separar diccionari
        working-directory: diccionaris
        run: python "separar_arxiu (per canvis a diccionari_txt).py"

# ----- versions de les columnes (i comprovació que van totes a l'una)
#       Ha d'anar ABANS del comit: si les columnes no quadren, el script
#       peta, el workflow s'atura i no es publica un diccionari trencat.
      - name: Actualitzar versions.json
        working-directory: diccionaris
        run: python generar_versions.py

# ----- comit columnes diccionari i diccionari (i versions.json)
      - name: Comit diccionari i columnes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "Separació automàtica del diccionari" || echo "No s'han detectat canvis nous per guardar"
          git push


# ===================
          
          
# ------ Generar resultat_ordenat_cons.json
      - name: Generar json de rimes sencer
        working-directory: bot
        run: python "generador_rimes_cons.py"

# ----- comit resultat_ordenat_cons.json
      - name: Comit json de rimes sencer
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "Generació automàtica del json de rimes sencer" || echo "No s'han detectat canvis nous per guardar"
          git push

# ------ Crear llista nàufragues
      - name: Crear llista de naufragues
        working-directory: llistes
        run: python generar_naufragues.py

# ----- comit naufragues
      - name: Comit llista de nàufragues
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "Generació automàtica de la llista de nàufragues" || echo "No s'han detectat canvis nous per guardar"
          git push

# ------ Crear llistes mots_de7
      - name: Crear llista de mots_de7
        working-directory: llistes
        run: | 
          python generar_mots_de7_real.py
          python generar_mots_de7_glosa.py

# ----- comit mots_de7 
      - name: Comit llista de mots de 7
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "Generació automàtica de la llista de mots de 7" || echo "No s'han detectat canvis nous per guardar"
          git push